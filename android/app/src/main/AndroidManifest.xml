<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- приложение читает обычную папку в памяти телефона -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/Theme.MathMark">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="uiMode|orientation|screenSize|screenLayout|density|fontScale"
            android:theme="@style/Theme.MathMark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Отдаёт инструкцию для нейросети по запросу:
              content query, uri content://dev.yury.mathmark/prompt
          Наружу уходит только текст промпта. Ни файлов, ни настроек.
        -->
        <provider
            android:name=".PromptProvider"
            android:authorities="dev.yury.mathmark"
            android:exported="true"
            android:grantUriPermissions="false" />
    </application>
</manifest>
